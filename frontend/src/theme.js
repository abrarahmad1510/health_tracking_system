import { createTheme } from '@mui/material/styles';

export const medTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#00f2c3',
    },
    secondary: {
      main: '#ff4d8f',
    },
    background: {
      default: '#0a1929',
      paper: '#132f4c',
    },
  },
});
